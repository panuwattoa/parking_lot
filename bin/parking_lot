#!/bin/bash
package_name=$1
if [[ "$OSTYPE" == "linux-gnu"* ]]; then
    platforms="linux"
elif [[ "$OSTYPE" == "darwin"* ]]; then
    platforms="darwin"
fi
if [[ -z "$package_name" ]]; then
./parkinglot-$platforms-amd64
else
./parkinglot-$platforms-amd64 $package_name
fi